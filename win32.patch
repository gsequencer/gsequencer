diff --git a/Makefile.am b/Makefile.am
index fa8ededca..b19cde9e8 100644
--- a/Makefile.am
+++ b/Makefile.am
@@ -8,9 +8,9 @@
 AUTOMAKE_OPTIONS = foreign
 SUBDIRS = po
 
-# if WITH_GNULIB
-# SUBDIRS += lib
-# endif
+if WITH_GNULIB
+SUBDIRS += lib
+endif
 
 ACLOCAL_AMFLAGS = -I m4
 AM_CPPFLAGS = -std=gnu99 -include errno.h
diff --git a/configure.ac b/configure.ac
index cad86e8be..e5242c99b 100644
--- a/configure.ac
+++ b/configure.ac
@@ -122,7 +122,7 @@ AC_ARG_ENABLE(experimental, [AS_HELP_STRING([--enable-experimental],
 
 # Checks for programs.
 AC_PROG_CC
-# gl_EARLY
+gl_EARLY
 AC_PROG_CPP
 AC_PROG_CXX
 AC_PROG_INSTALL
@@ -405,11 +405,12 @@ AGS_DEVELOPER_BOOK_LISTINGS([])
 
 AGS_DOCS_API([])
 
-# gl_INIT
+gl_INIT
 
 AM_EXTRA_RECURSIVE_TARGETS([ags-docs])
 AC_CONFIG_FILES([
 Makefile
+lib/Makefile
 unit-system-tests.mk
 functional-system-tests.mk
 docs/reference/libags/Makefile
@@ -422,6 +423,5 @@ libags.pc
 libags_audio.pc
 libags_gui.pc
 libgsequencer.pc])
-# lib/Makefile
 
 AC_OUTPUT
